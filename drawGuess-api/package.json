{
  "name": "cloud-paper-api",
  "version": "1.0.0",
  "description": "cloud-paper-api",
  "main": "api_router.js",
  "scripts": {
    "start": "nodemon lib/app.js --exec babel-node",
    "build": "babel lib -d dist",
    "serve": "node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lilu",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "express": "^4.15.2"
  }
}
