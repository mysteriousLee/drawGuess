export default class DrawInfo {
  constructor (obj) {
    this.width = obj.width;
    this.height = obj.height;
    this.timeStamp = obj.timeStamp;
    this.type = obj.type;
    this.x = obj.x;
    this.y = obj.y;
    this.pen = obj.pen;
  }
}
